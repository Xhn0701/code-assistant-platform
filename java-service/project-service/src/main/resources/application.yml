server:
  port: 8081
spring:
  application:
    name: project-service
  datasource:
    url: jdbc:postgresql://localhost:5432/code_assistant
    username: postgres
    password: postgres123
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
management:
  endpoints:
    web:
      exposure:
        include: health,info
