# Docker 环境专用配置
spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:postgresql://postgres:5432/code_assistant
    username: postgres
    password: postgres123

  redis:
    host: redis
    port: 6379

# Python Agent服务配置
agent-service:
  url: http://agent-service:8000
